[project]
name = "apify-linkedin-learning-api-example"
version = "1.0.0"
description = "Python quick-start for the LinkedIn Learning API on Apify"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apify-client>=3,<4",
    "python-dotenv>=1.0,<2",
]

[tool.uv]
package = false
